{
  "name": "receipts-buffer-only-mvp",
  "version": "1.0.0",
  "private": true,
  "description": "A Buffer-only client approval layer: sync posts, share a review room, stamp approval, and keep the receipt.",
  "scripts": {
    "check": "node scripts/check.js",
    "test": "npm run check && node scripts/test-http.js && node scripts/test-frictionless-beta.js && node scripts/test-buffer.js && node scripts/test-demo-flow.js && node scripts/test-review-snapshot.js && node scripts/test-email.js",
    "serve": "npx --yes serve . -l 8888"
  },
  "engines": {
    "node": ">=18"
  }
}
